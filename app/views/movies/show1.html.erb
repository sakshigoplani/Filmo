<% if @movies.search_movies.empty? %>
<p> No Records Found </p>
<% else %>
<div class="div">

  <table border="3">
    <thead>
      <tr>
        <th>Title</th>
        <th>Poster url</th>
        <th>Genres</th>
        <th>Year</th>
        <th>Imdb rating</th>
        <th>Length</th>
        <th>Studio</th>
        <th>Price</th>
        <th colspan="3"></th>
      </tr>
    </thead>
<% @movies.search_movies.each do |c| %>
<br>
    <tbody>
        <tr>
          <td><%= c.title %></td>
          <td> <img src="<%= c.poster_url %>" alt="HTML5 Icon" style="width:128px;height:128px;" /> </td>
          <td><%= c.genres %></td>
          <td><%= c.year %></td>
          <td><%= c.imdb_rating %></td>
          <td><%= c.length %></td>
          <td><%= c.studio %></td>
          <td><%= c.price %></td>
          <td><%= link_to 'Show', movie %></td>
  <!--         <td><%= link_to 'Edit', edit_movie_path(movie) %></td> -->
  <!--        <td><%= link_to 'Destroy', movie, method: :delete, data: { confirm: 'Are you sure?' } %></td> -->
  	<td><a href="/cart/<%=movie.id%>">Add to cart</a><td>
        </tr>
      <% end %>
    </tbody>
  </table>
